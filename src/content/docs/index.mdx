---
title: Trang Giới Thiệu
description: Website in ấn đẹp, nhanh, chuyên nghiệp tại Tây Ninh
---

import { FaPrint, FaBolt, FaSmile } from "react-icons/fa";
import { motion, AnimatePresence } from "framer-motion";
import { useState } from "react";
import { useRouter } from "next/router"; // Nếu dùng Astro thì đổi sang navigate API tương ứng

export default function HomePage() {
  const router = useRouter();
  const [isExiting, setIsExiting] = useState(false);

  const handleNavigate = (url) => {
    setIsExiting(true);
    setTimeout(() => router.push(url), 5
